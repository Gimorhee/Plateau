import React from 'react'
import PropTypes from 'prop-types'

const MyCart = ({ match }) => {
    return (
        <div>
            My Cart
        </div>
    )
}

MyCart.propTypes = {

}

export default MyCart
